export default class Vendor {
	constructor(id, name, prefix, merchants, isActive) {
		this.id = id;
		this.name = name;
		this.prefix = prefix;
		this.merchants = merchants;
		this.isActive = isActive;
	}
}
